# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(odometryTaskLib)
ADD_LIBRARY(${ODOMETRY_TASKLIB_NAME} SHARED 
    ${ODOMETRY_TASKLIB_SOURCES})

add_dependencies(${ODOMETRY_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${ODOMETRY_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ODOMETRY_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ODOMETRY_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ODOMETRY_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ODOMETRY_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ODOMETRY_TASKLIB_HEADERS}
    DESTINATION include/orocos/odometry)

